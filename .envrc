# Toolchain via flake.nix: ocaml + dune + clickhouse (for the round-trip tests).
# No opam switch needed — the library is stdlib-only and the C stubs link
# nothing beyond libc.
use flake

source_env_if_exists .envrc.local
